# Static hosting (direct browser API requests): leave unset.
# Managed server: set before npm run build to use /api/catchment.
# VITE_DATA_MODE=server

# Server runtime configuration
PORT=3000
HYDROGUARD_DATA_DIR=./data
